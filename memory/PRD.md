# Guard Dog — PRD / Memory

## Original problem statement (summary)
Frozen M1 execution spec (Part 4.1 Android real blocking proof + Part 5 FastAPI signed rules / threat intelligence). Preserve public SDK boundary (`requestPermission("vpn")`, `startProtection()`), truthfulness rule (THREAT_BLOCKED only from observed + dropped packet), privacy-first local-first boundary, canonical repository file plan, no parallel trees.

## User choices (2026-06)
- Scaffold canonical monorepo in /app from scratch; task board at docs/M1_TASK_BOARD.md; implement directly.
- Controlled endpoint placeholders via env: m1-block-test.guarddog.example / 203.0.113.10 (not acceptance targets).
- Google Web Risk: provider abstraction + env key wiring + mocked tests; no live key.
- Fresh M1 test-only Ed25519 keypair `gd-m1-test-ed25519-001` (+ rollover key 002); private keys in gitignored backend/.env; .env.example without secrets.
- Kotlin/Swift: complete source + tests, labelled code-review ready / not runtime-verified.

## Architecture
- backend/app (FastAPI, motor): routes health/config/rules/keys/intelligence; services jcs (rfc8785), rule_signer, key_registry, rule_bundle, provider_cache, intelligence; providers base + google_webrisk; repositories for the 5 allowed Mongo collections. server.py re-exports app.main:app.
- packages/guarddog-contracts: TS contracts; synced to frontend/src/contracts/shared by script.
- Android: guarddog-core (platform-agnostic; verifier, canonicalizer, engine, abstractions) ← guarddog-vpn (service, /32 route, TUN reader, drop reporter, deduper) ← expo module (records, adapters, module class).
- iOS: GuardDogCore (verifier, JCS, canonicalizer, CapabilityProvider) ← GuardDogNetworkFeasibility; Expo iOS module + DTO adapters (analysis-only, never THREAT_BLOCKED).
- apps/guarddog-mobile == frontend (symlink): SDK, contracts, harness, index.tsx harness screen.

## Implemented (2026-06)
- All spec files created (see docs/M1_TASK_BOARD.md). Backend 93 pytest passing; contracts 14 node tests passing; fixtures generated; web harness verified via screenshot (BLOCKED/SKIPPED honest states).
- Native gate round: signing guard (admin token + GD_SIGNING_ENABLED + confirm + ruleset allow-list + controlled-config check; secret redaction), endpoint verify/resign scripts, Gradle root + CI gate scripts (android/ios), Expo config plugin linking the Android SDK modules, android-dev-build script with merged-manifest checks, TunSession recovery + tests, proof report export (JSON + PDF via expo-print), docs/M1_NATIVE_GATE.md (status OPEN).

- 2026-06 (Elastic IP confirmed by user): real bundle RESIGNED → final v25 (v18 first; v19–24 were legacy live-test bundles, guarded resign re-run), keyId gd-m1-test-ed25519-001, payloadHash 2581666c…; `scripts/verify_resigned_bundle.py` 10/10 PASS (docs/evidence/resigned-bundle-verification.json). Recovery checklist (approved, AC-06) implemented: native `RecoveryInspector`/`getRecoveryStatus` (TUN closed, lifecycle, OS VPN transport), harness steps stop/tun-closed/route-cleared/recovered (real HTTPS 200 required), E2E test step 6, `TunSessionRecoveryTest` case, docs/M1_RECOVERY_CHECKLIST.md. Proof report m1-2: recovery chain + local JSON file (expo-file-system) + PDF + share sheet (expo-sharing). Static native-gate review recorded in docs/M1_NATIVE_GATE.md (gates remain OPEN).

- User review (2026-06): APPROVED, M1 OPEN pending native execution. Three checks applied: (1) `getRecoveryStatus` removed from public `GuardDogSecuritySDK`; harness-only adapter `src/harness/recoveryDiagnostics.ts`; (2) TRANSPORT_VPN is supporting evidence only (Kotlin `RecoveryStatus.recovered` excludes it; harness `route-cleared` = !selectiveRouteActive && !tunOpen; E2E logs it, does not assert); (3) bundle FROZEN at v25: `GD_M1_FROZEN_BUNDLE_VERSION=25` → API 409 BUNDLE_FROZEN, resign script needs `--unfreeze`, verify script asserts ==25, conftest blanks the freeze for isolated in-process tests. Revoke-path proof approved (device). User wants the Android native gate output first, before device time.

- 2026-06 Android native gate EXECUTED here (installed JDK17 arm64 + Android SDK + Gradle 8.9 + qemu x86_64 aapt2 via ~/.gradle/gradle.properties override): PASSED, 29 native tests 0 failed, expo prebuild + :guarddog-expo-module:assembleDebug compiled. Fixed by real build: erdtman coordinate io.github.erdtman; `fun interface ProtectionEnforcementReporter`; RuleBundleVerifier raw-JSON type check ("3"/3.0 → SCHEMA_INVALID parity); config plugin adds kotlin-serialization classpath to app root build.gradle; gate script hardened. Evidence docs/evidence/android-native-gate.txt (+ test XMLs). frontend/android generated dir gitignored. User audits this before iOS gate / device work.

- 2026-06 reviewer: AC-01 APPROVED; asked to verify generated app SDK levels. Found minSdk 24 → fixed with expo-build-properties (26/36/36), gate step 6 asserts it; standalone SDK aligned to AGP 8.12.0 / Kotlin 2.1.20 / compileSdk 36 / Gradle 8.13; gate re-run PASSED (29/29) from clean prebuild. Added scripts/ci/setup-android-toolchain-linux.sh (system packages in this container are NOT persistent — /opt and apt installs vanished once; re-run setup before gate). iOS gate (AC-02) needs macOS/Xcode — cannot run here (GuardDogCore imports CryptoKit; no Swift toolchain).

- 2026-06 AC-04 track: android-dev-build.sh ran here through merged-manifest audit → PASS (all reviewer checklist items; high-risk perms absent; READ/WRITE_EXTERNAL_STORAGE blocked via app.json blockedPermissions; android.permissions now INTERNET/ACCESS_NETWORK_STATE/FGS/FGS_SYSTEM_EXEMPTED/POST_NOTIFICATIONS). APK assembly NOT completed here: NDK/CMake x86_64-only → qemu wrappers (-0 argv0) worked but the container system layer resets periodically (JDK/SDK/qemu/tmp wiped twice) — not viable for long builds. Added GitHub Actions jobs: android-dev-build (ubuntu, uploads APK + manifest evidence), ios (macos-15, Xcode select, prebuild ios, same gate script, evidence artifact). setup-android-toolchain-linux.sh: pinned versions + SHA-256 verification + NDK/CMake qemu wrapping on arm64. Script fixes: merged_manifest path (AGP 8.12), no-device graceful exit, GD_DEV_ABIS default arm64-v8a.

- 2026-06 reviewer stop: android-dev-build workflow must NOT receive the signing private key → fixed: job needs only var GD_BACKEND_URL; dev-build script verifies endpoint via public /api/config (`verify_controlled_endpoint.py --api`) and asserts served bundle == frozen version + signed by SDK-pinned public key; no backend started in that job. Backend distribution-only mode added (GD_SIGNING_ENABLED=false → private key optional, no seed signing, can_sign=False). The separate `backend` pytest job still uses the key secret for in-process signing tests (backend side, not mobile) — candidate to switch to an ephemeral CI seed later. Release manifest must prove SYSTEM_ALERT_WINDOW absent (debug-only).

- 2026-06: reviewer approved trust-boundary fix. Added: workflow now has ZERO secrets (executable-suites uses ephemeral per-run Ed25519 seeds + GD_CI_EPHEMERAL_KEY=1 skips the 2 real-key tests); security/frozen/controlled-bundle-v25.json + tests/test_frozen_bundle_public_verification.py (public-key-only verification of frozen v25); scripts/ci/apk-recheck.sh (dev APK recheck: package/debuggable/26/36/arm64-v8a/VpnService facts/no secrets packaged) wired after the APK build. GitHub Actions cannot be triggered from this environment — user/engineering must push + run `native-gates`; next audit items: ios-native-gate.txt, android-dev-build artifacts (apk-recheck.txt, merged-manifest-audit.txt).

- 2026-06 provenance chain (reviewer request): apk-recheck records apkSha256 + commit + run id (apk-provenance.json); android-dev-build bakes EXPO_PUBLIC_GIT_SHA/CI_RUN_ID; Expo module AsyncFunction getBuildProvenance (Android hashes installed base APK off-thread; iOS identity only); harness adapter src/harness/buildProvenance.ts; ProofReport m1-3 has `provenance` + PDF section; index.tsx shows APK SHA + commit/run. Workflow permissions contents: read. Release-manifest audit deferred to after/parallel with device proof (user scope call). NEXT: user runs GitHub gates; agent audits ios-native-gate.txt, apk-recheck.txt, merged-manifest-audit.txt.

- 2026-06 CHECKPOINT (reviewer-confirmed, no further code changes until CI evidence): AC-01 PASS · AC-04 manifest/build audit PASS · bundle v25 FROZEN · CI provenance READY · AC-02 PENDING real macOS CI · dev APK PENDING CI artifact · physical proof PENDING artifact audit · M1 OPEN. Operational handoff: user does Save to GitHub → sets repo variable GD_BACKEND_URL (only) → runs native-gates → attaches ios-native-gate.txt, apk-recheck.txt, apk-provenance.json, merged-manifest-audit.txt, Android gate evidence, failed-job logs. Agent audits: jobs genuinely ran; AC-02 compiled/tests passed; v25 consumed; pinned key correct; APK arm64-v8a + debuggable; permissions clean; apkSha256 is a valid 64-hex digest tied to commit/run. NO sideload before that audit; phone must reproduce the exact apkSha256.

- 2026-06 first GitHub run audit → fixes: wrapper JAR committed (Gradle 8.13); emergentintegrations + private litellm wheel removed from requirements (unused); ios-native-gate.sh rewritten (full xcodebuild log to evidence via pipefail+tee, failing-phase context, toolchain + yarn why + expo install --check audit, clears expo-modules-jsi/apple/Products cache); Expo set aligned via `expo install --fix` (expo 57.0.20, expo-router 57.0.19, modules-core 57.0.16, modules-jsi 57.0.8); ios job Xcode pinned 26.1 (workflow_dispatch input `xcode`). Android gate re-run PASSED with new packages. User must re-run all three GitHub jobs.

## Backlog / remaining
- **M2.1 is now FROZEN PASS** (see "M2.1 — FROZEN PASS (2026-09, final)" entry below for full provenance). Do not touch M2.1 enforcement/acceptance logic further absent a real regression.
- **P0 (current)**: out-of-band Private DNS/DoH characterization — scope not yet detailed, clarify with user before starting.
- Live signing tests gated by GD_RUN_LIVE_TESTS=1. Order agreed with user: Android native gate → iOS native gate → Android dev build → physical-device proof → recovery proof → export JSON/PDF → final M1 acceptance. Do not mark native gates or M1 complete without native execution evidence.
- Out of scope for M1 (user decision): backend evidence upload/archiving of proof reports. Local JSON + PDF export only.
- P1: SharedPreferences store hardening (EncryptedSharedPreferences), backend admin auth beyond static token, provider retry/backoff.
- P2: iOS enforcement feasibility beyond M1 (out of scope), Threat Scent UX.
- P2: Android Native Consolidation (`com.guarddog.*` vs `com.hucentai.apollosecurity` duplication, Phase 5.2) — flagged, not decided/acted on, STRICTLY post-M2.1.

## M1 sign-off (2026 session)
User formally signed off and FROZE M1 (commit 32e29c4, CI run #24 green). `docs/M1_CI_RUNBOOK.md` carries the final attestation. No further M1 code changes; the invariant (`THREAT_BLOCKED` only from a real TUN packet drop, never a DNS/rule/cache/cloud match alone) is now a hard constraint for all M2 work too.

## M2.1 — Production Protection: Real Threat Intelligence + Website & Browser Gate
Objective: additive, DNS-triggered sinkhole to block malicious websites using local signed rules + Google Web Risk (key held off by user decision — provider stays fail-open/unavailable). Same invariant as M1: a DNS/rule/threat-intel match alone must NEVER emit THREAT_BLOCKED; only an authorized real TUN packet drop can. Design frozen in `docs/M2_WEBSITE_GATE_DESIGN.md`.

Build order (all approved by user, phase-by-phase): Phase 1 backend ruleset → Phase 2 shared contracts/capabilities → Phase 3 `guarddog-core` parallel authorization table → Phase 4 `guarddog-vpn` DNS/sinkhole plumbing → Phase 5 bridge/app wiring + local override store → Phase 6 native tests + physical-device proof.

- Phase 1 (backend, complete): new isolated ruleset `gd-m2-website-gate` (own bundle/version history), `local_verdict`/`IntelligenceService.lookup` ruleset-parameterized (M1 default unchanged), `localRulesExpired` flag, `Verdict` gains `warn` (unused so far), `/api/config` exposes `gateGuard.websiteGateRulesetId`. 95 backend tests passing; M1 v25 unchanged.
- Phase 2 (shared contracts, complete): `PlatformCapabilityProfile` + `ANDROID_M1_CAPABILITY_PROFILE`/`ANDROID_M2_CAPABILITIES` (differ by exactly `dnsVisibility`) in `guarddog-contracts` + mirrored to `frontend/src/contracts/shared`; `SecurityEvent` +9 additive optional fields + `verdict: "warn"`, mirrored in Kotlin. 19/19 TS tests pass.
- Phase 3 (`guarddog-core`, complete): `WebsiteGateBinding`/`WebsiteGateAuthorization` + parallel `websiteGateBindings` table in `GuardDogSDKEngine`, fully independent from the M1 `authorization` field/table. `acceptWebsiteGateRuleBundle`/`authorizeWebsiteGateTarget` (DNS arms a binding, emits nothing) + `reportBlockedPacket` extended to fall through to the M2 table only when the M1 table doesn't cover the destination (M1 branch textually unchanged). New `BlockedThreatEvidence.ENFORCEMENT_LAYER_ANDROID_DNS_SINKHOLE_DROP`. New `GuardDogSDKEngineTest.kt` (first JVM test for this class) + live-signed fixture `security/test-vectors/m2-website-gate/m2_website_gate_valid_bundle.json`.
- Phase 4 (`guarddog-vpn` DNS/sinkhole plumbing, complete + native-CI verified): new pure-Kotlin classes `DnsPacketClassifier`, `DnsQueryParser`, `SinkholeBindingStore` (the shared-core decision wiring), `DnsResponseSynthesizer` (only place that writes back into the TUN — real IPv4/UDP+checksums), `WebsiteGatePacketAuthorizer`, `DnsGatewayPacketHandler` (thin orchestrator), `UpstreamDnsForwarder`/`SocketProtector`/`ProtectedUdpDnsForwarder` (real upstream forward over a `VpnService.protect()`-ed socket), `WebsiteGateAddressing`/`WebsiteGateRouteConfig` (fixed RFC 5737 TEST-NET-1 pool: `192.0.2.53` DNS gateway, `192.0.2.240-243` sinkhole — never RFC1918, never loopback). Additive-only edits (all verified to preserve exact M1 behavior when new params are left null/default): `Ipv4PacketParser.udpPayloadRange()`, `PacketDropReporter` (+`websiteGateAuthorizer` param), `TunPacketReader` (+`output`/`dnsGatewayIpv4`/`dnsGateway` params), `SelectiveRouteInstaller` (`isSelective` generalized to "every route is a /32, never a default route"; new `buildWebsiteGateSpec()`, `buildSpec()` untouched), `GuardDogVpnService`/`GuardDogVpnRuntime` (new optional M2 runtime fields, `establish()` branches on them). Full new test suite (pure JVM/kotlin.test): `DnsPacketClassifierTest`, `DnsQueryParserTest`, `DnsResponseSynthesizerTest`, `WebsiteGatePacketAuthorizerTest`, `WebsiteGateAddressingTest`, `SinkholeBindingStoreTest`, `DnsGatewayPacketHandlerTest`, `ProtectedUdpDnsForwarderTest` (real loopback resolver), `PacketDropReporterTest` (new), `TunPacketReaderDnsGatewayTest` (new); additive tests appended to `Ipv4PacketParserTest`/`SelectiveRouteInstallerTest`.
  - **native-gates CI run #34252761281 (`m2-native-acceptance`, commit `1071e73`): SUCCESS, all 6 jobs green** (android 7m25s, ios 19m44s, executable-suites 1m0s, android-dev-build 11m36s, Gate Guard 13m52s, android-startup-smoke 14m44s). `guarddog-core`+`guarddog-vpn` combined: **111 unit tests run, 0 failed, 0 errors** across 23 test classes (48 of the 111 are new Phase 4 test methods). Expo Android module: compiles, `definition()` evaluates (2/2), 0 reified stubs, frozen SDK levels confirmed (minSdk 26/compileSdk 36/targetSdk 36). `== ANDROID NATIVE GATE PASSED ==`.
  - Fixed en route to green (first run #34247850291 on the same branch had failed on these two pre-existing gaps, not Phase 4 regressions — that first run had already proven, before failing, that all Phase 4 production Kotlin compiles cleanly via `assembleRelease --quiet` with zero errors): (1) `GuardDogSDKEngineTest.dnsMatchAloneNeverProducesThreatBlocked_onlyReportBlockedPacketCan` had a pre-existing Phase-3 bad assertion (`emitted.size == 0`, not accounting for the legitimate `RULE_BUNDLE_ACCEPTED` audit event) — narrowed to check only for absence of `THREAT_BLOCKED`/`THREAT_DETECTED`, matching 3 sibling tests already in the same file. (2) `backend/.env.example`'s M2 signing allow-list existed in the sandbox from Phase 1 but had never been committed (git status showed it modified across many prior sessions) — committed explicitly, fixing `executable-suites`' `test_website_gate_ruleset.py` 403s.
- Phase 5 (bridge/app wiring + local override store, complete — local checks green; native-gates CI run pending): `WebsiteGateOverrideStore` (`guarddog-vpn`: `ALLOW`/`NONE` only, no `BLOCK` value exists in the type) wired as an additive param into `SinkholeBindingStore.arm()` — an ALLOW override short-circuits before the rule-authority chain, so it can never arm a binding (4 new `SinkholeBindingStoreTest` cases + 6 new `WebsiteGateOverrideStoreTest` cases). `GuardDogExpoModule` gets 6 additive sync functions (`configureWebsiteGate`, `acceptWebsiteGateRuleBundle`, `getWebsiteGateStatus`, `setWebsiteGateAllowOverride`, `getWebsiteGateOverrides`, `clearWebsiteGateOverrides`); `GuardDogExpoModuleDefinitionTest` asserts the exact registered function set. New shared-contracts module `websiteGateOverrides.ts` (`guarddog-contracts/src`, synced to `frontend/src/contracts/shared`): durable override record `{ host, type: "allow", source: "user", decidedAt }` — `type` is a 1-value literal union, structurally excluding any "block" variant; bounded to `MAX_WEBSITE_GATE_OVERRIDES = 500` (oldest-evicted); 11 new `node --test` cases incl. an explicit test that a record tampered to `type: "block"` is rejected by the validator. New `frontend/src/sdk/websiteGateOverrides.ts` (thin AsyncStorage glue, the durable source of truth across restarts — the native store is session-only, matching `GuardDogVpnRuntime.config` re-push semantics). `GuardDogSecuritySDK.ts` gains the matching public wrapper surface + `getPlatformCapabilityProfile()` (returns `ANDROID_M2_CAPABILITIES` only when `getWebsiteGateStatus().dnsGatewayActive` is live-true, else M1 profile, `null` off-Android) — zero M1 methods touched. Verified locally: `guarddog-contracts` 30/30 `node --test` (was 19/19), ESLint clean, Expo web bundle rebuilds clean (953 modules), M1 harness screen boots with no console errors. **No `gh` CLI / git remote configured in this sandbox — the agent cannot trigger the `native-gates` GitHub Actions run itself; the user must push/trigger it (as with every prior phase) before Phase 5 can be called native-verified.** Consumer UI ("Apollo's Patrol") wiring stays out of scope per design doc §7 — no harness screen (`frontend/app/index.tsx`) was changed.
- Phase 5.1 (Android capability-truth correction, complete, `guarddog-contracts` only — NOT a shared-schema redesign): `ANDROID_M2_CAPABILITIES.dnsVisibility`/`domainVisibility` values were NOT flipped to `false` (would erase the milestone's real capability gain) and no field was added to `PlatformCapabilityProfile` (would be exactly the shared-schema change this correction must avoid). Instead: generalized the interface's doc comments to state every boolean is scoped to what the platform's own mechanism actually observes, expanded `ANDROID_M2_CAPABILITIES`'s comment to name the two real bypasses (Android Private DNS/DoT; app-embedded DoH — both invisible to Apollo's plaintext-UDP/53 sinkhole), and added a new exported, testable string constant `ANDROID_M2_DNS_VISIBILITY_SCOPE` stating the limitation for any consumer to quote verbatim. 1 new test (31/31 total, was 30/30) locks in the values stayed true/true and asserts the new constant names both bypasses + explicitly disclaims system-wide/universal coverage. No Kotlin/Swift file touched (this capability is synthesized JS-side from a live native flag). **Git note**: sandbox connected to `github.com/zelnix/Apollo`; verified via `git ls-remote` that this sandbox's prior HEAD (`908a8d0`) directly descends from the real `m2-native-acceptance` branch tip (`1071e733a0b0f0443c4fb834a1871d7d995d1ad3`, confirmed byte-identical) with 2 unpushed commits — no divergence; local branch renamed `main` → `m2-native-acceptance` to match, `origin` remote added (fetch/push not yet performed).
- Phase 5.2 (architectural duplication flagged, NOT reconciled): fetched `origin/main` and discovered it ships its OWN independent native Android VPN/DNS-filter implementation — `com.hucentai.apollosecurity` (`frontend/modules/apollo-security/android`, its own `PlatformCapabilityProfile`/`SecurityPlatformAdapter` TS contract using a tri-state `CapabilityLevel` + `ProtectionStatus.coverageScope: string[]`) — entirely separate from Gate Guard's `com.guarddog.*` stack in this repo. This was not previously known (main had only been reviewed for shared-contract changes, not identified as also having a second native VPN engine). Per explicit product decision: flagged as a duplication/coordination issue for the product owner to resolve in a future **Android Native Consolidation** milestone (current lean: Gate Guard becomes canonical, `com.hucentai.apollosecurity` retired/thinned — not decided or acted on). Main's `PlatformCapabilityProfile.ts`/`SecurityPlatformAdapter.ts` were deliberately NOT copied/wired into this branch. Gate Guard's own boolean-only capability contract was kept as-is (no redesign); instead `ANDROID_M2_DNS_VISIBILITY_SCOPE` now names the concrete scope tag `"dns:udp-53"` inline, and a new standalone constant `ANDROID_M2_DNS_COVERAGE_TAG = "dns:udp-53"` was added (same tag vocabulary as main's `coverageScope`, but not that type, not imported, not wired into anything shared). 2 new tests (32/32 total). Reminder recorded for Phase 6: the physical-device acceptance backend must run main's newly-landed Biting/Truth-of-State backend invariant, and Expo Go/web preview cannot validate the native DNS/VPN path — a generated native Android dev/release build is required.
- Phase 6 (native tests + physical-device proof): in-app harness screen COMPLETE + frontend-verified (screenshot + `testing_agent`, web preview only); actual physical-device run itself NOT started (device testing pending). **Phase 5 + 5.1 + 5.2 declared native-verified**: `native-gates` CI run [34295478834](https://github.com/zelnix/Apollo/actions/runs/34295478834) — Status: Success, commit `4966da4d45f02c293fd319afca5e425cce08c5d0` on `m2-native-acceptance`, all 6 jobs green (android, ios, executable-suites, android-dev-build, Gate Guard, android-startup-smoke), 20m23s, 5 artifacts (`android-dev-build` 42.5MB, `gate-guard` 66.1MB, `android-native-gate`, `android-startup-smoke`, `ios-native-gate`). This sandbox's local HEAD is exactly this commit (verified, clean tree). Fillable Phase 6 acceptance report template prepared at `docs/M2_PHASE6_ACCEPTANCE_TEMPLATE.md` — frozen invariant at top ("THREAT_BLOCKED is evidence-backed only"), full provenance/evidence-chain/PASS-FAIL-matrix (4 groups: positive enforcement, negative false-Biting, recovery/stop/revoke, Private DNS/DoT/DoH) + 3-way overall verdict (PASS / FAIL / PASS WITH DOCUMENTED CAPABILITY GAP).
  - **In-app runner built**: `frontend/app/phase6-acceptance.tsx` — mirrors the template field-for-field: auto-capture provenance (commit SHA/CI run id/APK SHA-256/version/build number via `readBuildProvenance()`, device-confirmed active native stack + manufacturer/model/OS/security patch via new Kotlin `getPhase6DeviceProvenance()`, ruleset/bundle info from the live backend bundle, network type via `expo-network`), manual-entry fields for what Android does not expose to third-party apps (Private DNS setting, branch/job/artifact names, backend provenance), "Activate Website Gate" (configure + accept bundle), guided Group-1 positive-enforcement runner (`runPositiveEnforcementTest` — real `fetch()`, observes the SDK's own event stream, never fabricates a THREAT_BLOCKED) and Group-2 negative-false-Biting runners (5 kinds: rule-match-alone, manual-override, local-analysis-only, gate-start-alone, failed-dns-forward) in `phase6WebsiteGateHarness.ts`, manual-step snapshot capture for Groups 3/4 (stop/revoke/restart/network-transition — Android doesn't let a third-party app trigger these itself, so the screen instructs the tester and then snapshots `getProtectionState()`/`getWebsiteGateStatus()` after each manual step), a fillable PASS/FAIL/CAPTURED/BYPASSED/UNOBSERVABLE matrix (4 groups, chip pickers, evidence-ref + notes per row), known-capability-gaps section (Private DNS/DoT + app-embedded DoH, pre-filled with the disclosed scope tags), and a 3-way verdict picker (PASS / FAIL / PASS WITH DOCUMENTED CAPABILITY GAP) — **the screen itself never computes or infers a verdict; every result/verdict field is exactly what the tester taps/types**. Export evolved from the original "copy to clipboard" idea to **Generate PDF + native share sheet** (`phase6AcceptanceReport.ts`, `expo-print` + `expo-file-system`, mirrors the template as styled HTML) — a more robust, shareable evidence artifact than clipboard text, kept as the primary export path.
  - **Bug fixed + wired this session**: `readBuildProvenance()` was called without `await` in the auto-capture mutation (commit/CI/APK fields would silently never populate on a real device); fixed. The screen was also an orphan route (no in-app link) — added an "Open Phase 6 Acceptance Report Runner" entry point on the main harness screen (`index.tsx`) with working back-navigation. Both verified via screenshot + `testing_agent` frontend pass (web preview): navigation, auto-capture (native-only fields correctly show "unavailable" placeholders off-device, non-native fields like network type populate), matrix/verdict chip toggling, text inputs, no crashes/console errors.
  - **Second CI issue found + fixed (2026-06, same day, after yarn.lock fix confirmed working)**: `native-gates` run [34355349738](https://github.com/zelnix/Apollo/actions/runs/34355349738) — the yarn.lock fix worked (`android`, `ios`, `executable-suites`, `android-dev-build`, `android-startup-smoke` all PASSED with real build durations this time). Only `Gate Guard` (the packaged RELEASE manifest audit, `scripts/ci/gate-guard-audit.sh`) failed, at `bash scripts/ci/gate-guard-audit.sh` (exit 1), after a real ~13m41s build. **Root cause**: `expo-network` (added by the same earlier, never-fully-committed session that built the Phase 6 harness — confirmed via `git log -S'"expo-network"'`: it first appears in THIS session's own initial auto-commit `ad884a6`, i.e. it was never present at the last verified-passing commit `4966da4`, so this is the first `native-gates` run to ever package it) ships its own `AndroidManifest.xml` declaring `android.permission.ACCESS_WIFI_STATE` (confirmed by reading `frontend/node_modules/expo-network/android/src/main/AndroidManifest.xml` directly) — a normal, non-dangerous Android permission it needs to distinguish wifi/cellular for `Network.getNetworkStateAsync()` (used by the Phase 6 harness's "Network type (auto)" field). `scripts/ci/gate_guard_audit.py`'s strict release-manifest allow/deny list predates this dependency and had `ACCESS_WIFI_STATE` sitting on the `DENY_LIST` (a blanket-deny added long before `expo-network` existed in this app) — so the release build now legitimately fails the audit's "every requested permission is on the allow-list" + "absent: ACCESS_WIFI_STATE" checks. **Not a build/compile failure** — `android`/`ios`/`android-dev-build` all compile and pass fine; only this one stricter release-only permission audit caught the new dependency, exactly as its own docstring says it's designed to ("any new transitive dependency permission is an audit FAIL until reviewed here"). **Fixed**: moved `android.permission.ACCESS_WIFI_STATE` from `DENY_LIST` to `ALLOWED_PERMISSIONS` in `scripts/ci/gate_guard_audit.py` with an explicit origin comment (`expo-network (framework; Phase 6 harness reads network type...)`); `gate_guard_audit.py --selftest` still passes (fixtures unaffected). Checked `scripts/ci/apk-recheck.sh` (the separate dev-build merged-manifest check, which already passed this run) for the same gap — its own high-risk deny-list never included `ACCESS_WIFI_STATE`, so no change needed there. **Cannot verify end-to-end locally** — this sandbox has no Android SDK/JDK/Gradle to actually build the release APK/AAB and re-run the real audit against it; the fix is verified only at the allow-list-logic level (selftest + direct read of expo-network's shipped manifest). Explicitly `git add`+`git commit`-ed (learned from the yarn.lock incident above — raw `git stash` edits can be silently dropped by the turn-end auto-commit). User needs to push/trigger `native-gates` again for the real `Gate Guard` result.
  - **CI GREEN AGAIN (2026-06)**: `native-gates` run [34422964124](https://github.com/zelnix/Apollo/actions/runs/34422964124) — Status: **Success**, commit `09405a1f3f201c1ab5bd4b7fc764c1d06489533e` on `m2-native-acceptance`, all 6 jobs green (android 9m1s, ios 16m12s, executable-suites 49s, android-dev-build 13m25s, Gate Guard 14m54s, android-startup-smoke 15m37s), 24m44s total, 5 artifacts. Only Node20/setup-java deprecation warnings, zero errors. This confirms both CI fixes above (yarn.lock `expo-network` sync + `ACCESS_WIFI_STATE` allow-listing) fully resolved the regression, and the Phase 6 harness UI changes (nav wiring + `await` fix) are now native-verified end-to-end on top of them. **Phase 5 + 5.1 + 5.2 + Phase 6 harness UI all native-verified as of this commit.**
  - **CI GREEN again after UX pass (2026-06)**: `native-gates` run [34431010032](https://github.com/zelnix/Apollo/actions/runs/34431010032) — Status: **Success**, commit `8d300b1`, all 6 jobs green (android 7m9s, ios 22m19s, executable-suites 54s, android-dev-build 12m26s, Gate Guard 14m10s, android-startup-smoke 12m12s), 22m29s total, 5 artifacts. Only benign Node20 deprecation warnings. Confirms the Phase 6 harness UX overhaul (Live status card, STEP 1-13 badges, manual/auto field highlighting, test-domain autofill, restructured Groups 3&4, matrix/gaps/verdict instructions) shipped this session doesn't break anything native-side.
  - **Phase 6 harness UX pass (2026-06)**: added a "Live protection status" card (live StatusBadge for protection state / gate configured / DNS gateway active, with contextual "NEEDS TO BE ON"/"NEEDS TO BE OFF" callouts + one-tap quick-action buttons); explicit "STEP 1"-"STEP 13" labels on every actionable button with an independent green-check/red-X status badge per action (themed background+foreground pair, not raw colored text, so it's visible in both light and dark theme); manual-entry fields visually highlighted (warning-colored border + "type this in" tag) vs auto-filled fields, with "optional" tags + example placeholders on every previously-blank field; "Authorized test domain"/"Matching rule ID" now auto-fill from the live M2 bundle's first block rule (backend `gd-m2-website-gate` ruleset was signed to bundleVersion 3 with a real block rule for `blocktest.btciq.app`, the user's real controlled test domain, via `POST /api/rules/sign` -- same real domain as the M1 controlled endpoint, now also live in the M2 bundle for physical DNS/sinkhole/TUN testing); Groups 3&4 manual-step section restructured so each of the 4 scenarios (Stop protection, VPN revoke, app restart, network transition) has its own co-located instruction + its own "Capture snapshot" button (Stop protection ordered before Capture snapshot per user request); PASS/FAIL matrix, capability gaps, and overall verdict sections each got a "HOW TO DETERMINE..." instructional callout with concrete decision criteria. Regression-verified by `testing_agent`: all STEP badges present and independently tracked, native-dependent actions fail gracefully (no crash) in web preview, matrix/verdict chips + text inputs all functional, navigation intact. (Note: mid-session a screenshot-tool sandbox session on this agent's side got stuck showing `useQuery` as permanently "pending" for the config/bundle fetch; `testing_agent` could not reproduce this on 3 fresh reloads -- confirmed as an isolated tooling artifact, not an app bug, no code changed for it.)
  - **CI regression found + fixed (2026-06, after user pushed commit `36be3a1` to GitHub)**: `native-gates` run [34332031276](https://github.com/zelnix/Apollo/actions/runs/34332031276) — `android` AND `ios` jobs BOTH failed at the identical first step (`yarn install --frozen-lockfile && ...`), before `expo prebuild` or any native-gate script ever ran; `executable-suites` ran (45s, no failure annotation); `android-dev-build`/`Gate Guard`/`android-startup-smoke` never ran (0s, workflow-dependency skip). **Root cause**: `frontend/package.json` requires `expo-network@~57.0.1` (added by the prior "ci: audit fixes — ... Expo SDK 57 patch alignment" session), but the committed `frontend/yarn.lock` was never updated with the matching resolved entry — `yarn install --frozen-lockfile` correctly refuses to silently mutate the lockfile in CI, so it hard-fails with `error Your lockfile needs to be updated`. **Not caused by this session's Phase 6 changes** (those two `.tsx` edits contain no dependency changes) — reproduced locally by stashing this session's already-present-but-uncommitted `yarn.lock` fix and re-running `yarn install --frozen-lockfile` against the exact committed HEAD (`36be3a1`), which failed identically; restoring the fix and re-running passes (`success Already up-to-date`). The `android-native-gate.txt`/`core-deps.txt`/`vpn-deps.txt` artifacts the user attached from that run are **stale evidence from the last PASSING run**, re-uploaded by an `if: always()` artifact step reading a committed `docs/evidence/` file that this run's script never actually regenerated (that script never ran — the job died one step earlier). **Fixed**: `frontend/yarn.lock` now carries the `expo-network@57.0.1` resolved entry; verified locally that `yarn install --frozen-lockfile` passes clean. **Correction (2026-06, same day)**: the first attempt at this fix (commit `149997b`) did NOT actually persist — the working-tree `yarn.lock` edit had been made via raw `git stash`/`stash pop` in a bash call rather than an explicit `git add`+`git commit`, and the platform's auto-commit-on-turn-end only picked up the unrelated `memory/PRD.md` edit that turn, silently dropping the yarn.lock diff. Confirmed by re-running the exact same `native-gates` failure at the next pushed commit (`560619b`) — identical `yarn install --frozen-lockfile` failure, identical stale artifact re-upload. Re-diagnosed, re-applied, and this time explicitly `git add frontend/yarn.lock && git commit` (commit `a0bef7a`) so it cannot be silently dropped again; re-verified `yarn install --frozen-lockfile` passes against this committed HEAD. User needs to push/trigger `native-gates` again for a real (non-stale) android/ios gate result — watch for the SAME failure signature (both jobs dying at the identical first step) in case this recurs, which would indicate a still-deeper lockfile drift rather than this specific missing entry.

- **Phase 6A rebuilt as an automated harness + human-in-the-loop UX (2026-09), then run on a physical device — RESULT: frozen only as a narrower "Truth-of-State enforcement baseline", NOT full M2.1 acceptance:**
  - User pivoted Phase 6 from a manual fillable-matrix screen (`phase6-acceptance.tsx`, kept as "Advanced/Diagnostics") to a new automated orchestrator `phase6-automated.tsx` + `src/harness/phase6AutomatedHarness.ts` (deterministic state machine owning every PASS/FAIL/PRECONDITION_FAILURE verdict — the tester never interprets logs) + `src/harness/phase6AutomatedReport.ts` (PDF/JSON export). Fixed an infinite loop in the `dns-capability` handoff (earlier this session).
  - **UX overhaul (this session)**: fixed a real bug (an earlier "quick fix" referenced an undeclared `setFeedback()` — would throw at runtime). Rebuilt around a frozen visual-hierarchy contract: action-needed card → detection sub-state → "What Apollo is doing" → "What happens next?" progress panel (step X of 12) → Technical Evidence (collapsed by default, "N checks"). Every manual OS-only action (VPN revoke, app restart, network toggle) now drives explicit `ACTION_REQUIRED` (numbered steps + "Open Settings" via `Linking.sendIntent` to the exact Android settings screen) → `WAITING_FOR_CONFIRMATION` → `ACTION_CONFIRMED` ("✓ Done — Apollo detected the change correctly", held ~2s) → `CONTINUING`, or a **non-fatal** `WAITING_FOR_CONFIRMATION` → `ACTION_NOT_DETECTED` (45s timeout, `awaitingSince`/`awaitingAttempts` — UI-only, never read by `finalizeRun`/any verdict) with a contextual hint + Try again/Open Settings. "No action needed from you" banner shown during fully-automated phases.
  - **Report/export integrity fix**: added `isFinalRun()` (phase `done` AND non-null `overallVerdict`) as the single completeness gate; export JSON is now wrapped with `reportKind: FINAL_ACCEPTANCE_RESULT | INTERIM_DIAGNOSTIC_ONLY` + `runId`/`exportedAt`, PDF gets a loud "INCOMPLETE / INTERIM DIAGNOSTIC — NOT AN ACCEPTANCE VERDICT" banner whenever not final, filenames get an `-INTERIM-DIAGNOSTIC` suffix — always the current run only, never any state from the legacy manual `/phase6-acceptance` screen (separate AsyncStorage key + separate report builder, confirmed by code review). Verified by `testing_agent` (web-preview-reachable paths only — no bugs found) before push.
  - **native-gates CI run [34667685678](https://github.com/zelnix/Apollo/actions/runs/34667685678)**: commit `90ad089e09b217911a8c0a0f1c7b6b41859db807` on `m2-native-acceptance` — **Success**, all 6 jobs green (android 6m8s, ios 22m4s, executable-suites 51s, android-dev-build 11m7s, Gate Guard 13m4s, android-startup-smoke 14m0s), 22m14s total, 5 artifacts (`android-dev-build` 42.6MB used for the device run below). Agent-verified: fetched the pushed commit directly from GitHub and diffed it against the local pre-push commit (`57f95d2`) — the 3 changed files are byte-for-byte identical; only `.emergent/emergent.yml` + the added test-report JSON differ repo-wide. Confirms Save-to-GitHub preserved content exactly despite re-committing under a new SHA.
  - **Physical-device run (Pixel 10, Android 17, real hardware, run `phase6a-1789183661331`)**: exported report `reportKind: FINAL_ACCEPTANCE_RESULT`, provenance `gitSha`/`ciRunId` exactly match the CI run above (provenance loop closed, confirmed before trusting the run) — **overall verdict PASS** (`ALL_REQUIRED_ROWS_PASSED`), `truthOfStateViolation: false`. All 6 preconditions PASS. **Core result genuinely proven**: `1.1` positive enforcement — real `THREAT_BLOCKED` (`enforcementEvidenceId 89ff6e0e-...`) with native counters `observedMatching 0→1`, `droppedMatching 0→1`, `reportedBlocks 0→1` (a real packet observed + intentionally dropped, not a rule-match fabrication). All 5 Group-2 false-Biting negative cases (`2.1`-`2.5`: rule-match-alone, manual-override, local-analysis-only, gate-start-alone, failed-dns-forward) correctly produced **no** `THREAT_BLOCKED` within their windows. State-machine UX worked in production exactly as designed: VPN-revoke polling detected the action in ~39s (6× "not detected yet" log lines every ~4s, comfortably inside the 45s `ACTION_NOT_DETECTED` threshold — tester likely never saw that recovery card), app-restart detected instantly on relaunch, network-toggle detected in ~8s. `4.1` DNS-capability correctly stayed `UNOBSERVABLE` (`NO_INDEPENDENT_EVIDENCE_EITHER_WAY` — DNS resolution failed outright, no independent proof either way, not guessed as a bypass) and `4.doh` stayed `NOT_TESTABLE` — exactly the "never guess" contract agreed earlier.
  - **User's acceptance ruling (explicit, do not weaken)**: this run is frozen ONLY as the **"Phase 6A physical-device Truth-of-State enforcement baseline: PASS"** — commit `90ad089e09b217911a8c0a0f1c7b6b41859db807`, CI run `34667685678`. It is **NOT** the full M2.1 acceptance baseline. Four rows are broader than their actual evidence and must be tightened before full M2.1 acceptance: **`1.2`** (marked PASS without a genuinely separate second resolution/request — currently just re-asserts `1.1`'s evidence), **`3.2`** (VPN-revoke row: the restart-rejection was correctly observed, but the captured status snapshot still reported `consentGranted: true`, which is inconsistent with a genuine revoke and needs its own truthful assertion), **`3.3`** (restart row proved a clean inactive state but did NOT explicitly prove override *rehydration* — the row's own title claims more than what was checked), **`3.4`** (the observed transition was `CELLULAR → NONE` while protection was already inactive — not an active Wi-Fi↔cellular transition while protection is running, which is what the row is meant to exercise). Private DNS (`4.1` UNOBSERVABLE) and DoH (`4.doh` NOT_TESTABLE) remain open capability gaps, unchanged.
  - **NEXT WORK ORDER (explicit user instruction — do not reorder)**: tighten the test methodology/assertions for rows `1.2`, `3.2`, `3.3`, `3.4` in `phase6AutomatedHarness.ts` FIRST, re-run physical-device Phase 6A, THEN address DNS/DoH out-of-band coverage — not DNS-only work next. Only once those 4 rows are corrected and re-run clean should M2.1 be considered fully frozen.

- **Rows 1.2 / 3.2 / 3.3 / 3.4 tightened per user-frozen assertion definitions (2026-09, same day), + new row 3.5 (do not weaken these definitions without an explicit user decision):**
  - Added `"NOT_TESTED"` as a 7th `Phase6StepVerdict` (distinct from `NOT_TESTABLE`/`UNOBSERVABLE`: means "this row's own evidence genuinely couldn't be collected this run for reasons unrelated to enforcement correctness — never a synonym for PASS or FAIL, never silently upgraded"), and `"PASS_WITH_UNVERIFIED_ROWS"` as a new overall verdict (`finalizeRun`: any group-1/2/3 row left `NOT_TESTED` forces this instead of `PASS`, checked before the existing `PASS_WITH_CAPABILITY_GAP`/group-4 path). Added the harness-wide rule as a frozen file-header comment: **a row may only return PASS from evidence collected specifically for that row — no inherited PASS, no inferred PASS, no "same as previous row" shortcut.**
  - **`1.2`** — now performs a genuinely SEPARATE second fetch to the same `testDomain` immediately after `1.1` (same live binding, own fresh TUN observation via `getEnforcementStats()` before/after). PASS requires a fresh TUN observation/drop AND (a new `enforcementEvidenceId` distinct from `1.1`'s, OR `dedupedRetries` incremented with no new event — a correctly-deduplicated retry). No TUN observation → `NOT_TESTED` (`SECOND_REQUEST_NOT_INDEPENDENTLY_OBSERVED`); observation with neither a new record nor a deduped-retry accounting → `FAIL` (`SECOND_OBSERVATION_UNACCOUNTED`). No longer inherits `1.1`'s verdict.
  - **`3.2`** — PASS now requires 5 INDEPENDENTLY-verified conditions, deliberately never `consentGranted` (confirmed stale on the physical run): `state!==ACTIVE`, `tunOpen===false`, `selectiveRouteActive===false`, restart-without-consent rejected, AND no stale active/biting state (`!tunOpen && !selectiveRouteActive && !dropReporterAttached && !vpnTransportPresent`). The `consentGranted:true`-after-revoke contradiction is now its own always-computed diagnostic (`consentGrantedFieldContradiction` in evidence + a dedicated `⚠ CONTRADICTION` run-log line + a `_WITH_STALE_CONSENT_FIELD` reasonCode suffix) — flagged loudly, never silently dropped, never allowed to weaken the 5 pass conditions.
  - **`3.3`** — split into two INDEPENDENT checks, both required for PASS (`evaluateAwaitingRestart`, now `async`): **(A)** no orphaned active/TUN-open state in the fresh process (the old, weaker whole-row definition); **(B)** override rehydration — row `3.2` now creates a dedicated disposable probe host (`phase6a-override-probe.<testDomain>`, never the real test domain) right before instructing the restart; after relaunch, `3.3` verifies the durable JS record survived (`GuardDogSecuritySDK.getWebsiteGateOverrides()`) AND the native ephemeral session cache actually got re-populated by `hydrateWebsiteGateOverrides()` (new harness-only read `recoveryDiagnostics.readNativeWebsiteGateOverrides()` → raw `GuardDogNative.getWebsiteGateOverrides()`), then unconditionally removes the probe. B not set up → `3.3` overall `NOT_TESTED`; B ran and failed → `FAIL`; A failed → `FAIL` regardless of B.
  - **`3.4`** — no longer accepts `CELLULAR → NONE` while already inactive as a valid transition. Added a new automatic phase `reactivate-for-network` (runs right after `3.3`, before the tester touches the network) that re-does consent/config/rule-bundle-accept/hydrate-overrides/`startProtection`/gate-active-poll from scratch (native session state doesn't survive the row-3.3 restart) — MAY trigger one more real Android VPN-consent system dialog, expected/unavoidable. If reactivation fails for any reason, `3.4`+`3.5` are honestly `NOT_TESTED` (`skipNetworkActiveTest`) and the run continues to `dns-capability` rather than hard-stopping. The tester now must land on a genuinely DIFFERENT connected type (Wi-Fi ↔ cellular, not just off) while protection is ACTIVE. Two explicit, evidence-gated outcomes: if it stays `ACTIVE` post-transition, PASS requires `tunOpen===true` AND `dnsGatewayActive===true` AND no stale evidence during the transition itself; if it drops to non-ACTIVE, PASS requires that to be reported truthfully (TUN genuinely closed, no stale evidence) — an explicitly accepted, documented outcome, not a guess about design intent either way.
  - **New row `3.5`** — post-transition positive sanity check, own fresh fetch/TUN evidence, only runs (and only meaningful) when `3.4` confirms a genuinely truthful ACTIVE recovery; otherwise `NOT_TESTED` (not applicable).
  - Verified: `yarn tsc --noEmit` clean on all 3 touched files (only pre-existing, unrelated repo-wide `.ts`-import-extension + `theme.ts` errors remain, untouched by this change), ESLint clean, `testing_agent` regression pass on the web-reachable surface (Ready-to-run → PRECONDITION_FAILURE → Result → Technical Evidence → export → reset → legacy-screen — zero bugs; the actual new row logic is native-only and out of scope for web preview by design).
  - **STILL PENDING (do not skip)**: push → `native-gates` CI on the exact commit → focused physical-device re-run of rows `1.2`/`3.2`/`3.3`/`3.4`/`3.5` (this WILL prompt for VPN consent a second time mid-run at `reactivate-for-network` — expected) → only then reconsider the M2.1-freeze decision. DNS/DoH out-of-band coverage remains the task AFTER this.

## M2.1 — FROZEN PASS (2026-09, final)
Groups 1-3 (rows 1.1-3.5) physically re-verified clean after the tightened assertions above; two remaining P0 gaps closed this round, then re-verified end-to-end:
- **Native fix**: `VpnStateRepository.kt` `consentGranted` was going stale (`true`) after a genuine OS-level VPN revoke when no live `VpnService` instance observed `onRevoke()`. Fixed by wiring a live `osConsentCheck` (`VpnService.prepare(context) == null`) that `current()` re-derives from on every query (`GuardDogExpoModule.kt`), never trusting the cache alone when Android itself can be asked — falls back to cache only if the live check throws. 3 new Kotlin unit tests (`VpnLifecycleStateTest.kt`).
- **Row 4.1 hardening**: previously reused the same shared `blocktest.btciq.app` host as rows 1.1/1.2/3.x, so leftover background retries could contaminate its raw `observedMatching` counter delta → false FAILs. Fixed: added a fully dedicated, additively-signed rule `m2-block-dns-capability-001` → `dnsprobe.blocktest.btciq.app` to the `gd-m2-website-gate` bundle (now v4; existing 3 rules incl. frozen `m2-block-blocktest-001` carried forward byte-for-byte via `backend/scripts/add_dns_capability_rule.py`, idempotent). `phase6AutomatedHarness.ts` row 4.1 now decides PASS/FAIL/CAPABILITY_GAP/UNOBSERVABLE strictly from the `THREAT_BLOCKED` event's own `host`+`ruleId` fields for that dedicated probe only — never the shared global counter.
- **CI**: first post-fix run [34677676047](https://github.com/zelnix/Apollo/actions/runs/34677676047) (commit `61eb60c`=`c4528a4`) was 5/6 green; `executable-suites` failed on an unrelated new backend test file (`test_m2_dns_capability_rule.py`) that made live HTTP calls to a non-existent-in-CI URL + hardcoded an absolute bundle version/pinned pubkey — rewritten to the suite's real in-process `client`/`admin_headers` fixture pattern with relative version deltas + dynamically-fetched pubkey (commit `562f0cc`). Clean 6/6 green confirmed on **[34680021418](https://github.com/zelnix/Apollo/actions/runs/34680021418)**, commit `140de1c` (verified byte-identical code to `562f0cc` via commit diff — only `.emergent/emergent.yml` timestamp differs).
- **Physical-device run (Pixel 10, Android 17)** against that exact CI baseline — user-reported canonical JSON/PDF result:
  - `3.2` consent-revoke: `consentGranted:false`, restart-without-consent rejected, `consentGrantedFieldContradiction:false` (the earlier stale-field contradiction is gone).
  - `1.1` sanity anchor: real TUN observation + intentional drop + evidence-backed `THREAT_BLOCKED`.
  - `4.1` dedicated probe: `dnsprobe.blocktest.btciq.app`/`m2-block-dns-capability-001` produced its own attributable evidence-backed block.
  - `truthOfStateViolation: false`. **Overall verdict: PASS / ALL_REQUIRED_ROWS_PASSED.**
  - Provenance: Git SHA `140de1c340f127046591a0e15a262e05a25ce5ce`, CI run `34680021418`, Pixel 10 / Android 17, APK SHA-256 `5faaf3a77b8b0a250ed73f83f98bfbb4b63fe5c1245aca757ceb7dab82315ee5`.
- **User's final ruling: M2.1 = FROZEN PASS ✅.** Full acceptance (not just the narrower Truth-of-State baseline from the prior entry). Do not touch M2.1 enforcement/acceptance logic (`phase6AutomatedHarness.ts` rows, native VPN/DNS-gateway Kotlin, `gd-m2-website-gate`/`gd-m1-controlled-block` bundles) further unless a real regression is found.
- **NEXT WORK (explicit user direction)**: out-of-band Private DNS/DoH characterization (app-embedded DoH stays intentionally `NOT_TESTABLE` by the in-app harness — this is separate, out-of-band investigation/tooling, not another harness row), then the next Apollo milestone.
  - **Scope confirmed by user**: (1) deliverable = standalone diagnostic tool + a characterization doc generated from its results; (2) characterize BOTH Android system Private DNS/DoT (Off/Automatic/Strict hostname) AND app-embedded DoH (>=1 representative browser, DoH disabled vs enabled) — not exhaustive; (3) **observational only this milestone** — no port-853 blocking, no encrypted-DNS heuristics, no DoH interception, no VPN-route changes (a clearly separated "future mitigation candidates" section is fine, no implementation); (4) completely separate from frozen M2.1 — must not modify `phase6AutomatedHarness.ts`, M2.1 acceptance rows, frozen rule behavior, or existing enforcement semantics; (5) next formal milestone after this = **Android Native Consolidation Assessment** (decision milestone: com.guarddog.* vs com.hucentai.apollosecurity, ownership/dependency/migration-risk docs, retire-or-absorb decision) → then **iOS capability-profile milestone**. Email Guard/Call Guard/SMS Guard stay queued behind those.
  - **Built this round** (tool + doc, zero native/enforcement code changes, zero touches to any frozen M2.1 file): `frontend/src/diagnostics/dnsCapabilityDiagnostic.ts` (self-contained probe/observe/classify logic — does not import from `phase6AutomatedHarness.ts`; reuses the already-frozen, already-signed M2.1 rule `m2-block-dns-capability-001`→`dnsprobe.blocktest.btciq.app` **read-only** as the observation target, same strict host+ruleId attribution discipline as row 4.1), `frontend/src/diagnostics/dnsCapabilityDiagnosticReport.ts` (JSON/PDF export, separate from `phase6AutomatedReport.ts`), `frontend/app/dns-capability-diagnostic.tsx` (new screen, linked from a new, separate home-screen card — the existing M2.1 card/buttons untouched), `docs/dns-capability-characterization.md` (methodology + evidence schema + fillable results, mirrors the `M2_PHASE6_ACCEPTANCE_TEMPLATE.md` discipline). Verified via `tsc --noEmit`, ESLint, and `testing_agent` (frontend-only pass: navigation, RadioGroup/TextInput interactions, graceful `NATIVE_MODULE_UNAVAILABLE` on web, NOT_TESTABLE manual path, export gating, zero regression to the frozen M2.1 screens) — all green, no bugs found.
  - **STILL PENDING (user-side, physical device)**: run the DoT (3 modes) + DoH (>=1 browser, on/off) probes on a native Android build, export the JSON/PDF, fill in `docs/dns-capability-characterization.md` §4/§5 from that evidence.
  - **Evidence-model fix (before the Pixel 10 run, explicit user requirement)**: the original app-embedded DoH design relied on a manually-reported tester judgment ("did the page look like it loaded?") for the `BYPASSED` verdict — user flagged this as not strong enough evidence, reintroducing the exact human-interpretation problem M2.1 worked to remove. Fixed: DoH probes now generate a unique nonce, build a probe URL `https://dnsprobe.blocktest.btciq.app/dnsdiag/?n=<nonce>`, and poll a NEW, completely separate backend endpoint (`backend/app/api/routes/dns_diagnostics.py`, `/api/dns-diagnostics/receipts/{nonce}`, in-memory only, never touches M1/M2 rule data — 4/4 new pytest passing, 101 total backend tests passing) that the probe page calls client-side on load. `BYPASSED` is now only ever concluded from this independent server-verified receipt, never a manual report. Manual "Page loaded/did not load" RadioGroup removed entirely from the screen. DoT probes were already fully machine-observed (app's own `fetch()` + attributed event) and were left unchanged, per user confirmation.
  - **Pending user-side infra dependency (documented in `docs/dns-capability-characterization.md` §1a)**: `dnsprobe.blocktest.btciq.app` needs (1) a real DNS record + valid TLS cert coverage (else a genuine bypass fails at DNS/TLS and collapses into `UNOBSERVABLE`), and (2) one static HTML file hosted at `/dnsdiag/` that reads the `n` query param and POSTs it to the backend's receipt endpoint (reference implementation given in the doc — no custom backend logic needed on that host, CORS already open on our backend).
  - **Verified this round**: `tsc --noEmit` clean, ESLint clean, full backend pytest suite (101 passed/37 skipped) including new receipt endpoint tests, and a `testing_agent` pass covering both backend (endpoint correctness incl. duplicate-POST-keeps-original-timestamp, malformed-nonce 400s, frozen M1/M2 bundles unaffected) and frontend (manual RadioGroup confirmed removed, all other sections/regression intact) — no bugs found.
  - **Confirmed sequence for the physical run (user's exact order)**: `native-gates` CI on the diagnostic-only changes → install that CI APK on Pixel 10 → run Private DNS Off/Automatic/Strict → run representative browser DoH Off/On → export JSON/PDF → update the characterization doc → freeze the findings → THEN move to the Android Native Consolidation Assessment.
  - **Pre-Pixel-run infra checklist (user-specified) — status**: (1) `dnsprobe.blocktest.btciq.app` resolves publicly + valid HTTPS cert — **verified externally, already in place** (loads the same M1 controlled-endpoint content at root, no cert/interstitial error). (2) The dedicated `/dnsdiag/?n=<nonce>` static probe page — **still pending on the user's infra**, reference implementation with `Cache-Control: no-store` (both `<meta>` tag and `fetch(..., {cache:"no-store"})`) is in `docs/dns-capability-characterization.md` §1a. (3) The receipt endpoint stays HTTPS + externally reachable from a browser — **verified externally, working now** (`GET .../api/dns-diagnostics/receipts/<nonce>` returns the expected JSON). Added `Cache-Control: no-store` response headers on both `GET`/`POST` `/api/dns-diagnostics/receipts/{nonce}` and `cache:"no-store"` on the frontend's poll fetch, per user's caching concern. In-memory receipt store confirmed acceptable as-is (user's own risk analysis: a restart can only degrade a real bypass to `UNOBSERVABLE`, never fabricate `BYPASSED` — no persistence needed unless physical testing proves otherwise).


- **DNS/DoH Capability Diagnostic redesigned into a guided, evidence-automated wizard (2026-06, same milestone, before the physical Pixel run above):**
  - **Native (`GuardDogExpoModule.kt`, additive-only, does not touch M1/M2/Phase6 acceptance code)**: new sync `Function("getDnsCapabilityDeviceSnapshot")` returns `supportedAbis`/`primaryAbi`/`activeNativeStackId`, `networkTransport` (via `ConnectivityManager.getNetworkCapabilities`), `notificationsEnabled` (`NotificationManager.areNotificationsEnabled()`), and — the core addition — `privateDnsActive`/`privateDnsServerName`/`privateDnsRuntimeMode` read via `ConnectivityManager.getActiveNetwork()`→`getLinkProperties()`→`isPrivateDnsActive()`/`getPrivateDnsServerName()` (public API added in Android 9/API 28; guarded with an `SDK_INT>=28` check since this app's `minSdkVersion` is 26, else reports `UNSUPPORTED_OS_VERSION`). **Critical, deliberately preserved OS limitation**: `privateDnsRuntimeMode==="INACTIVE_OR_OFF"` is NEVER treated as proof of a deliberate "Off" — Android's public API cannot distinguish that from "Automatic" whose opportunistic DoT probe is currently failing/unreachable; every JS consumer of this field (`describePrivateDnsRuntimeMode`) states this ambiguity verbatim rather than collapsing it. `GuardDogExpoModuleDefinitionTest.kt`'s exact-`syncFunctions.keys` assertion updated to include the new function (would have failed CI otherwise). Mirrored type additions in both `frontend/src/sdk/nativeModule.ts` and `packages/guarddog-expo-module/src/index.ts` (kept in sync per existing file convention).
  - **New file `frontend/src/diagnostics/dnsCapabilityTruthSnapshot.ts`**: `captureDnsDiagnosticTruthSnapshot(probeRuleConfirmedInBundle)` aggregates a full machine-observed truth-of-state snapshot every time it's called (VPN consent, protection state, TUN/selective-route, notifications, Website Gate configured/dnsGatewayActive/acceptedRuleset+bundle+key, probe-rule-confirmed-in-bundle, active native stack, ABIs, Private DNS runtime mode+server name, network transport, full build provenance) — gracefully degrades to an honest all-null/`UNAVAILABLE` snapshot off-Android/no-native-module, never crashes. `computeTruthViolation` flags 3 contradiction cases (ACTIVE+consent-required; dnsGatewayActive+TUN-closed; probe-rule-not-confirmed) — surfaced, never hidden or silently corrected. `describePrivateDnsRuntimeMode()` is the single source of the user-approved honest wording for each runtime mode.
  - **`dnsCapabilityDiagnostic.ts` rewritten**: removed all manual radio-button/free-text Private DNS state entry. Added `CLASSIFICATION_LABELS` with the user's exact approved plain-language wording (CAPTURED="Apollo is biting — the request was observed and stopped", BYPASSED="This encrypted DNS request bypassed Apollo's DNS visibility", UNOBSERVABLE="Apollo couldn't prove what happened", NOT_TESTABLE="This check couldn't be run") — presentation-only, the underlying classification enum/evidence rules are unchanged. Added `DOT_WIZARD_STEPS` (Off/Automatic/Strict target-mode configs) and `pollForPrivateDnsRuntimeMode()` (continuous native polling, hard timeout `PRIVATE_DNS_POLL_HARD_TIMEOUT_MS=90_000`, UI-only "Still checking" hint at `PRIVATE_DNS_POLL_STILL_CHECKING_AFTER_MS=15_000` — changes no behavior). `runPrivateDnsProbeForStep()` derives `configurationLabel` strictly from a FRESH truth snapshot taken at probe-execution time (never the earlier poll-match snapshot); if the runtime state drifted between detection and execution, that drift is recorded verbatim in `notes`, never silently reported as the originally-expected state. Every `DnsDiagnosticRecord` now carries its own `truthSnapshot`. `ActivationResult` gained `preflightSnapshot` (captured at the end of Preflight, `probeRuleConfirmedInBundle` baseline carried into every later row this session).
  - **`app/dns-capability-diagnostic.tsx` rewritten as a 7-step guided wizard** (Preflight → Private DNS Off → Automatic → Strict → Browser DoH Off → Browser DoH On → Final report), replacing the old single-scroll manual checklist. **No "Continue anyway" escape hatch exists anywhere** (explicit user requirement) — if a DoT step's native polling times out at 90s, the ONLY offered actions are "Check again" / "Open Settings again" / "Record as not testable"; a row can never be labelled under a configuration the wizard couldn't verify. DoT steps auto-start polling on step entry and auto-run the probe + auto-advance-eligible the instant a genuine match is observed (zero manual taps needed beyond changing the OS setting and tapping the eventual "Continue" after results are shown). DoH steps (`doh-off`/`doh-on`) need exactly one manual tap ("I've changed it — Run probe", since Android cannot read another app's DoH setting at all) — after that, Apollo auto-generates a nonce, captures a pre-probe truth snapshot, `Linking.openURL`s the dedicated probe page, and auto-completes via an `AppState` 'active'-transition listener (with a 125s fallback timer as a safety net) — no second manual "Finish" tap. Preflight blocks all progression unless activation succeeds AND its truth snapshot has zero violations. Progress bar + "Step X of 7 · <title>" header.
  - **`dnsCapabilityDiagnosticReport.ts` extended**: `DnsCharacterizationRun` gained `preflightSnapshot`; JSON export naturally includes it + every record's `truthSnapshot` (no code change needed there beyond the type). PDF/HTML gained a full "Preflight — full automated truth-of-state snapshot" table (with a loud violation banner if any), a per-row "Truth violation" column, and each classification cell now shows the plain-language label under the raw enum value. Handles `preflightSnapshot: null` and empty `records[]` without throwing.
  - **Verified this round**: `tsc --noEmit` clean (only pre-existing, unrelated repo-wide errors remain — `theme.ts` + `.ts`-import-extension warnings, untouched), ESLint clean on all 5 touched/new files, screenshot-confirmed Preflight step renders correctly and honestly blocks progression with `NATIVE_MODULE_UNAVAILABLE` on web preview (no "Continue" button shown — correct, no escape hatch). `testing_agent` full pass: 6/6 backend regression tests green (receipt endpoint + M2.1 additive rule test, unchanged), confirmed zero diff under `src/harness/phase6*.ts`/`app/phase6-automated.tsx` (frozen M2.1 untouched), frontend Preflight-reachable flow correct, and a static code review of the unreachable-on-web wizard steps (poll/probe/truth-snapshot/DoH-AppState logic) found no bugs. **Steps 2-7 (DoT polling, DoH probes, final report) cannot be exercised live in Expo Go/web — this needs the same native Android build/physical-device path as the rest of Phase 6/M2.1; not yet run on a physical device.**
  - **NEXT (per user's own confirmed sequence, unchanged)**: `native-gates` CI on this diagnostic-only change → install the CI APK on Pixel 10 → run all 6 wizard steps live (Off/Automatic/Strict/DoH-Off/DoH-On/Final report) → export JSON/PDF → update `docs/dns-capability-characterization.md` with the new automated methodology → freeze the DNS/DoH characterization findings → THEN move to the Android Native Consolidation Assessment (`com.guarddog.*` vs `com.hucentai.apollosecurity`).

- **native-gates CI GREEN for the DNS/DoH wizard redesign**: run [34928269729](https://github.com/zelnix/Apollo/actions/runs/34928269729) — commit `34a9fec` on `m2-native-acceptance`, **Status: Success**, all 6 jobs green (android 7m10s, ios 20m33s, executable-suites 57s, android-dev-build 13m30s, Gate Guard 13m6s, android-startup-smoke 13m12s), 20m45s total, 5 artifacts (`android-dev-build` 42.7MB is the APK for the physical-device run). Only benign Node20/setup-java deprecation warnings, zero errors. Confirms the new native `getDnsCapabilityDeviceSnapshot()` (Kotlin) and updated `GuardDogExpoModuleDefinitionTest.kt` compile and pass cleanly end-to-end. **Next**: install this CI's `android-dev-build` artifact on the Pixel 10 and run the full 7-step wizard live, per the user's confirmed sequence — no further wizard changes until that physical evidence is back.

- **DNS/DoH Diagnostic Wizard: 4 evidence/truth fixes + 2 UX fixes from a physical-device code review (before any Pixel 10 run happened) — "NOT YET ready for physical characterization" verdict addressed:**
  1. **DNS caching across rows (biggest finding)**: every row used to reprobe the SAME shared hostname (`dnsprobe.blocktest.btciq.app`, M2.1's frozen row-4.1 host) — risked the OS/DNS resolver caching an earlier row's genuine bypass resolution and silently reusing it for a LATER row, making that later row falsely look like a bypass with no fresh DNS query. Fixed with a brand-new, independent ruleset `gd-m2-dns-diagnostic-wizard` (backend/scripts/create_dns_diagnostic_wizard_ruleset.py, idempotent/additive, signed+published this session — v1 live now) carrying 5 never-shared hostnames, one per row (`dnswiz-dot-off/-automatic/-strict/-doh-off/-doh-on`.blocktest.btciq.app`) — **deliberately a SEPARATE ruleset from the frozen `gd-m2-website-gate` v4**, per explicit instruction; the native engine only ever holds ONE accepted website-gate bundle at a time, so the wizard's Preflight now accepts THIS dedicated ruleset instead of the production one (architecturally clean — production enforcement is completely untouched). `GD_SIGNING_ALLOWED_RULESETS` in backend/.env + .env.example updated to allow it. **New infra dependency**: needs 4 NEW DNS A-records + TLS coverage (the 5th, `dnswiz-dot-off`, is also net-new — none reuse the existing `dnsprobe.` record) — likely covered automatically if `*.blocktest.btciq.app` already has wildcard DNS/TLS (as `dnsprobe.` suggests), but must be confirmed before the physical run. Documented in `docs/dns-capability-characterization.md` §1a.
  2. **"Off" row was not machine-verifiable but was being treated as if it were**: polling started immediately and matched `INACTIVE_OR_OFF` on entry, which is ALSO true if the device is actually Automatic-with-inactive-opportunistic-DoT — contradicting the code's own documented limitation. Fixed: `DotWizardStepConfig.targetRuntimeMode` is now `null` for the "Off" row; it never auto-polls-to-match. The tester manually confirms THEIR OWN selection ("I've set it to Off — Run probe"); Apollo then records the machine-observed state honestly and separately (`describePrivateDnsRuntimeMode`) — the UI/report NEVER says "Off verified". If the machine observes a state definitely inconsistent with Off (STRICT/ACTIVE_NO_HOSTNAME), that's a real contradiction, not a nuance — the row is force-gated to NOT_TESTABLE (see fix 3).
  3. **Truth snapshots were recorded but not a hard classification gate**: a row could still read CAPTURED/BYPASSED even if protection had stopped, TUN was closed, the DNS gateway was inactive, the probe rule wasn't confirmed, or Private DNS state drifted after polling — those were merely notes. Fixed via a new shared `evaluateTruthGate()`/`classify()` in `dnsCapabilityDiagnostic.ts`: any of those conditions now FORCE the row to `NOT_TESTABLE` with the reason(s) spelled out, overriding whatever the raw attribution/receipt signals would otherwise suggest. Also fixed the DoH contradiction case: if a row ever has BOTH a genuine attributed block event AND a confirmed server receipt (logically impossible together — a blocked request can't reach the page to fire its receipt), it's now gated to NOT_TESTABLE instead of ever displaying "Apollo is biting".
  4. **Native Private DNS read was inspecting Apollo's OWN VPN tunnel, not the real network**: `getDnsCapabilityDeviceSnapshot()` used `ConnectivityManager.activeNetwork`, which while Apollo's VpnService is active IS Apollo's own virtual network (Android routes default traffic through it) — its LinkProperties reflect the DNS servers Apollo's own `VpnService.Builder` declared, NOT the physical Wi-Fi/cellular network's real Private DNS state. Fixed in `GuardDogExpoModule.kt`: now selects the underlying network via `NetworkCapabilities.NET_CAPABILITY_NOT_VPN` + a physical transport (wifi/cellular/ethernet) + `NET_CAPABILITY_INTERNET`, falling back to `activeNetwork` only if no such network exists yet (e.g. before Preflight starts protection). This was a genuinely blocking correctness bug for every DoT row, since Preflight always starts the VPN before any DoT step runs.
  - **UX fixes**: (a) single "Share report" button replaces the separate Export JSON / Export PDF buttons (both files are still written to disk; only the PDF — the comprehensive, human-readable artifact — goes through the OS share sheet). (b) the DoH browser-name/version field is no longer reset between the "DoH Off" and "DoH On" steps (entered once, persists).
  - **Verified this round (QUICK/REGRESSION scope per the user's Credit-Efficient Testing Policy — NOT the full `testing_agent`, since the actual native/device behavior can't be exercised in Expo Go/web anyway)**: new backend test file `backend/tests/test_dns_diagnostic_wizard_ruleset.py` (3 tests: allow-listed + 5 distinct hosts, signing the wizard ruleset never touches `gd-m2-website-gate`'s version history, confirm-required enforced) — full backend suite 104 passed / 50 skipped, zero failures. `tsc --noEmit` + ESLint clean on all touched files. Screenshot-confirmed the updated Preflight copy renders correctly and still honestly blocks on web (`NATIVE_MODULE_UNAVAILABLE`). The new `create_dns_diagnostic_wizard_ruleset.py` script was actually RUN against this environment's live backend (same Mongo `native-gates` CI's `GD_BACKEND_URL` points at) — `gd-m2-dns-diagnostic-wizard` v1 is live now, confirmed idempotent on a second run.
  - **NOT YET done**: no native Android device/CI run of THESE 4 fixes specifically (native-gates needs to be re-triggered on this new commit before the physical Pixel 10 characterization, per the user's own stated sequence: "fix those four evidence/truth issues first, rerun native-gates, and then use that new APK for the Pixel 10"). The 4-new-hostname DNS/TLS infra dependency (§1a of the doc) is also still pending confirmation on the operator's side.
