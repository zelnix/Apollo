# Guard Dog — PRD / Memory

## Original problem statement (summary)
Frozen M1 execution spec (Part 4.1 Android real blocking proof + Part 5 FastAPI signed rules / threat intelligence). Preserve public SDK boundary (`requestPermission("vpn")`, `startProtection()`), truthfulness rule (THREAT_BLOCKED only from observed + dropped packet), privacy-first local-first boundary, canonical repository file plan, no parallel trees.

## User choices (2026-06)
- Scaffold canonical monorepo in /app from scratch; task board at docs/M1_TASK_BOARD.md; implement directly.
- Controlled endpoint placeholders via env: m1-block-test.guarddog.example / 203.0.113.10 (not acceptance targets).
- Google Web Risk: provider abstraction + env key wiring + mocked tests; no live key.
- Fresh M1 test-only Ed25519 keypair `gd-m1-test-ed25519-001` (+ rollover key 002); private keys in gitignored backend/.env; .env.example without secrets.
- Kotlin/Swift: complete source + tests, labelled code-review ready / not runtime-verified.

## Architecture
- backend/app (FastAPI, motor): routes health/config/rules/keys/intelligence; services jcs (rfc8785), rule_signer, key_registry, rule_bundle, provider_cache, intelligence; providers base + google_webrisk; repositories for the 5 allowed Mongo collections. server.py re-exports app.main:app.
- packages/guarddog-contracts: TS contracts; synced to frontend/src/contracts/shared by script.
- Android: guarddog-core (platform-agnostic; verifier, canonicalizer, engine, abstractions) ← guarddog-vpn (service, /32 route, TUN reader, drop reporter, deduper) ← expo module (records, adapters, module class).
- iOS: GuardDogCore (verifier, JCS, canonicalizer, CapabilityProvider) ← GuardDogNetworkFeasibility; Expo iOS module + DTO adapters (analysis-only, never THREAT_BLOCKED).
- apps/guarddog-mobile == frontend (symlink): SDK, contracts, harness, index.tsx harness screen.

## Implemented (2026-06)
- All spec files created (see docs/M1_TASK_BOARD.md). Backend 93 pytest passing; contracts 14 node tests passing; fixtures generated; web harness verified via screenshot (BLOCKED/SKIPPED honest states).
- Native gate round: signing guard (admin token + GD_SIGNING_ENABLED + confirm + ruleset allow-list + controlled-config check; secret redaction), endpoint verify/resign scripts, Gradle root + CI gate scripts (android/ios), Expo config plugin linking the Android SDK modules, android-dev-build script with merged-manifest checks, TunSession recovery + tests, proof report export (JSON + PDF via expo-print), docs/M1_NATIVE_GATE.md (status OPEN).

- 2026-06 (Elastic IP confirmed by user): real bundle RESIGNED → final v25 (v18 first; v19–24 were legacy live-test bundles, guarded resign re-run), keyId gd-m1-test-ed25519-001, payloadHash 2581666c…; `scripts/verify_resigned_bundle.py` 10/10 PASS (docs/evidence/resigned-bundle-verification.json). Recovery checklist (approved, AC-06) implemented: native `RecoveryInspector`/`getRecoveryStatus` (TUN closed, lifecycle, OS VPN transport), harness steps stop/tun-closed/route-cleared/recovered (real HTTPS 200 required), E2E test step 6, `TunSessionRecoveryTest` case, docs/M1_RECOVERY_CHECKLIST.md. Proof report m1-2: recovery chain + local JSON file (expo-file-system) + PDF + share sheet (expo-sharing). Static native-gate review recorded in docs/M1_NATIVE_GATE.md (gates remain OPEN).

- User review (2026-06): APPROVED, M1 OPEN pending native execution. Three checks applied: (1) `getRecoveryStatus` removed from public `GuardDogSecuritySDK`; harness-only adapter `src/harness/recoveryDiagnostics.ts`; (2) TRANSPORT_VPN is supporting evidence only (Kotlin `RecoveryStatus.recovered` excludes it; harness `route-cleared` = !selectiveRouteActive && !tunOpen; E2E logs it, does not assert); (3) bundle FROZEN at v25: `GD_M1_FROZEN_BUNDLE_VERSION=25` → API 409 BUNDLE_FROZEN, resign script needs `--unfreeze`, verify script asserts ==25, conftest blanks the freeze for isolated in-process tests. Revoke-path proof approved (device). User wants the Android native gate output first, before device time.

- 2026-06 Android native gate EXECUTED here (installed JDK17 arm64 + Android SDK + Gradle 8.9 + qemu x86_64 aapt2 via ~/.gradle/gradle.properties override): PASSED, 29 native tests 0 failed, expo prebuild + :guarddog-expo-module:assembleDebug compiled. Fixed by real build: erdtman coordinate io.github.erdtman; `fun interface ProtectionEnforcementReporter`; RuleBundleVerifier raw-JSON type check ("3"/3.0 → SCHEMA_INVALID parity); config plugin adds kotlin-serialization classpath to app root build.gradle; gate script hardened. Evidence docs/evidence/android-native-gate.txt (+ test XMLs). frontend/android generated dir gitignored. User audits this before iOS gate / device work.

- 2026-06 reviewer: AC-01 APPROVED; asked to verify generated app SDK levels. Found minSdk 24 → fixed with expo-build-properties (26/36/36), gate step 6 asserts it; standalone SDK aligned to AGP 8.12.0 / Kotlin 2.1.20 / compileSdk 36 / Gradle 8.13; gate re-run PASSED (29/29) from clean prebuild. Added scripts/ci/setup-android-toolchain-linux.sh (system packages in this container are NOT persistent — /opt and apt installs vanished once; re-run setup before gate). iOS gate (AC-02) needs macOS/Xcode — cannot run here (GuardDogCore imports CryptoKit; no Swift toolchain).

- 2026-06 AC-04 track: android-dev-build.sh ran here through merged-manifest audit → PASS (all reviewer checklist items; high-risk perms absent; READ/WRITE_EXTERNAL_STORAGE blocked via app.json blockedPermissions; android.permissions now INTERNET/ACCESS_NETWORK_STATE/FGS/FGS_SYSTEM_EXEMPTED/POST_NOTIFICATIONS). APK assembly NOT completed here: NDK/CMake x86_64-only → qemu wrappers (-0 argv0) worked but the container system layer resets periodically (JDK/SDK/qemu/tmp wiped twice) — not viable for long builds. Added GitHub Actions jobs: android-dev-build (ubuntu, uploads APK + manifest evidence), ios (macos-15, Xcode select, prebuild ios, same gate script, evidence artifact). setup-android-toolchain-linux.sh: pinned versions + SHA-256 verification + NDK/CMake qemu wrapping on arm64. Script fixes: merged_manifest path (AGP 8.12), no-device graceful exit, GD_DEV_ABIS default arm64-v8a.

- 2026-06 reviewer stop: android-dev-build workflow must NOT receive the signing private key → fixed: job needs only var GD_BACKEND_URL; dev-build script verifies endpoint via public /api/config (`verify_controlled_endpoint.py --api`) and asserts served bundle == frozen version + signed by SDK-pinned public key; no backend started in that job. Backend distribution-only mode added (GD_SIGNING_ENABLED=false → private key optional, no seed signing, can_sign=False). The separate `backend` pytest job still uses the key secret for in-process signing tests (backend side, not mobile) — candidate to switch to an ephemeral CI seed later. Release manifest must prove SYSTEM_ALERT_WINDOW absent (debug-only).

- 2026-06: reviewer approved trust-boundary fix. Added: workflow now has ZERO secrets (executable-suites uses ephemeral per-run Ed25519 seeds + GD_CI_EPHEMERAL_KEY=1 skips the 2 real-key tests); security/frozen/controlled-bundle-v25.json + tests/test_frozen_bundle_public_verification.py (public-key-only verification of frozen v25); scripts/ci/apk-recheck.sh (dev APK recheck: package/debuggable/26/36/arm64-v8a/VpnService facts/no secrets packaged) wired after the APK build. GitHub Actions cannot be triggered from this environment — user/engineering must push + run `native-gates`; next audit items: ios-native-gate.txt, android-dev-build artifacts (apk-recheck.txt, merged-manifest-audit.txt).

- 2026-06 provenance chain (reviewer request): apk-recheck records apkSha256 + commit + run id (apk-provenance.json); android-dev-build bakes EXPO_PUBLIC_GIT_SHA/CI_RUN_ID; Expo module AsyncFunction getBuildProvenance (Android hashes installed base APK off-thread; iOS identity only); harness adapter src/harness/buildProvenance.ts; ProofReport m1-3 has `provenance` + PDF section; index.tsx shows APK SHA + commit/run. Workflow permissions contents: read. Release-manifest audit deferred to after/parallel with device proof (user scope call). NEXT: user runs GitHub gates; agent audits ios-native-gate.txt, apk-recheck.txt, merged-manifest-audit.txt.

- 2026-06 CHECKPOINT (reviewer-confirmed, no further code changes until CI evidence): AC-01 PASS · AC-04 manifest/build audit PASS · bundle v25 FROZEN · CI provenance READY · AC-02 PENDING real macOS CI · dev APK PENDING CI artifact · physical proof PENDING artifact audit · M1 OPEN. Operational handoff: user does Save to GitHub → sets repo variable GD_BACKEND_URL (only) → runs native-gates → attaches ios-native-gate.txt, apk-recheck.txt, apk-provenance.json, merged-manifest-audit.txt, Android gate evidence, failed-job logs. Agent audits: jobs genuinely ran; AC-02 compiled/tests passed; v25 consumed; pinned key correct; APK arm64-v8a + debuggable; permissions clean; apkSha256 is a valid 64-hex digest tied to commit/run. NO sideload before that audit; phone must reproduce the exact apkSha256.

- 2026-06 first GitHub run audit → fixes: wrapper JAR committed (Gradle 8.13); emergentintegrations + private litellm wheel removed from requirements (unused); ios-native-gate.sh rewritten (full xcodebuild log to evidence via pipefail+tee, failing-phase context, toolchain + yarn why + expo install --check audit, clears expo-modules-jsi/apple/Products cache); Expo set aligned via `expo install --fix` (expo 57.0.20, expo-router 57.0.19, modules-core 57.0.16, modules-jsi 57.0.8); ios job Xcode pinned 26.1 (workflow_dispatch input `xcode`). Android gate re-run PASSED with new packages. User must re-run all three GitHub jobs.

## Backlog / remaining
- Live signing tests gated by GD_RUN_LIVE_TESTS=1. Order agreed with user: Android native gate → iOS native gate → Android dev build → physical-device proof → recovery proof → export JSON/PDF → final M1 acceptance. Do not mark native gates or M1 complete without native execution evidence.
- Out of scope for M1 (user decision): backend evidence upload/archiving of proof reports. Local JSON + PDF export only.
- P0 (native machine + physical device): run scripts/ci/android-native-gate.sh, ios-native-gate.sh, android-dev-build.sh; physical-device proof; recovery run; export report. M1 stays OPEN until then.
- P1: SharedPreferences store hardening (EncryptedSharedPreferences), backend admin auth beyond static token, provider retry/backoff.
- P2: iOS enforcement feasibility beyond M1 (out of scope), Threat Scent UX.

## M1 sign-off (2026 session)
User formally signed off and FROZE M1 (commit 32e29c4, CI run #24 green). `docs/M1_CI_RUNBOOK.md` carries the final attestation. No further M1 code changes; the invariant (`THREAT_BLOCKED` only from a real TUN packet drop, never a DNS/rule/cache/cloud match alone) is now a hard constraint for all M2 work too.

## M2.1 — Production Protection: Real Threat Intelligence + Website & Browser Gate
Objective: additive, DNS-triggered sinkhole to block malicious websites using local signed rules + Google Web Risk (key held off by user decision — provider stays fail-open/unavailable). Same invariant as M1: a DNS/rule/threat-intel match alone must NEVER emit THREAT_BLOCKED; only an authorized real TUN packet drop can. Design frozen in `docs/M2_WEBSITE_GATE_DESIGN.md`.

Build order (all approved by user, phase-by-phase): Phase 1 backend ruleset → Phase 2 shared contracts/capabilities → Phase 3 `guarddog-core` parallel authorization table → Phase 4 `guarddog-vpn` DNS/sinkhole plumbing → Phase 5 bridge/app wiring + local override store → Phase 6 native tests + physical-device proof.

- Phase 1 (backend, complete): new isolated ruleset `gd-m2-website-gate` (own bundle/version history), `local_verdict`/`IntelligenceService.lookup` ruleset-parameterized (M1 default unchanged), `localRulesExpired` flag, `Verdict` gains `warn` (unused so far), `/api/config` exposes `gateGuard.websiteGateRulesetId`. 95 backend tests passing; M1 v25 unchanged.
- Phase 2 (shared contracts, complete): `PlatformCapabilityProfile` + `ANDROID_M1_CAPABILITY_PROFILE`/`ANDROID_M2_CAPABILITIES` (differ by exactly `dnsVisibility`) in `guarddog-contracts` + mirrored to `frontend/src/contracts/shared`; `SecurityEvent` +9 additive optional fields + `verdict: "warn"`, mirrored in Kotlin. 19/19 TS tests pass.
- Phase 3 (`guarddog-core`, complete): `WebsiteGateBinding`/`WebsiteGateAuthorization` + parallel `websiteGateBindings` table in `GuardDogSDKEngine`, fully independent from the M1 `authorization` field/table. `acceptWebsiteGateRuleBundle`/`authorizeWebsiteGateTarget` (DNS arms a binding, emits nothing) + `reportBlockedPacket` extended to fall through to the M2 table only when the M1 table doesn't cover the destination (M1 branch textually unchanged). New `BlockedThreatEvidence.ENFORCEMENT_LAYER_ANDROID_DNS_SINKHOLE_DROP`. New `GuardDogSDKEngineTest.kt` (first JVM test for this class) + live-signed fixture `security/test-vectors/m2-website-gate/m2_website_gate_valid_bundle.json`.
- Phase 4 (`guarddog-vpn` DNS/sinkhole plumbing, complete + native-CI verified): new pure-Kotlin classes `DnsPacketClassifier`, `DnsQueryParser`, `SinkholeBindingStore` (the shared-core decision wiring), `DnsResponseSynthesizer` (only place that writes back into the TUN — real IPv4/UDP+checksums), `WebsiteGatePacketAuthorizer`, `DnsGatewayPacketHandler` (thin orchestrator), `UpstreamDnsForwarder`/`SocketProtector`/`ProtectedUdpDnsForwarder` (real upstream forward over a `VpnService.protect()`-ed socket), `WebsiteGateAddressing`/`WebsiteGateRouteConfig` (fixed RFC 5737 TEST-NET-1 pool: `192.0.2.53` DNS gateway, `192.0.2.240-243` sinkhole — never RFC1918, never loopback). Additive-only edits (all verified to preserve exact M1 behavior when new params are left null/default): `Ipv4PacketParser.udpPayloadRange()`, `PacketDropReporter` (+`websiteGateAuthorizer` param), `TunPacketReader` (+`output`/`dnsGatewayIpv4`/`dnsGateway` params), `SelectiveRouteInstaller` (`isSelective` generalized to "every route is a /32, never a default route"; new `buildWebsiteGateSpec()`, `buildSpec()` untouched), `GuardDogVpnService`/`GuardDogVpnRuntime` (new optional M2 runtime fields, `establish()` branches on them). Full new test suite (pure JVM/kotlin.test): `DnsPacketClassifierTest`, `DnsQueryParserTest`, `DnsResponseSynthesizerTest`, `WebsiteGatePacketAuthorizerTest`, `WebsiteGateAddressingTest`, `SinkholeBindingStoreTest`, `DnsGatewayPacketHandlerTest`, `ProtectedUdpDnsForwarderTest` (real loopback resolver), `PacketDropReporterTest` (new), `TunPacketReaderDnsGatewayTest` (new); additive tests appended to `Ipv4PacketParserTest`/`SelectiveRouteInstallerTest`.
  - **native-gates CI run #34252761281 (`m2-native-acceptance`, commit `1071e73`): SUCCESS, all 6 jobs green** (android 7m25s, ios 19m44s, executable-suites 1m0s, android-dev-build 11m36s, Gate Guard 13m52s, android-startup-smoke 14m44s). `guarddog-core`+`guarddog-vpn` combined: **111 unit tests run, 0 failed, 0 errors** across 23 test classes (48 of the 111 are new Phase 4 test methods). Expo Android module: compiles, `definition()` evaluates (2/2), 0 reified stubs, frozen SDK levels confirmed (minSdk 26/compileSdk 36/targetSdk 36). `== ANDROID NATIVE GATE PASSED ==`.
  - Fixed en route to green (first run #34247850291 on the same branch had failed on these two pre-existing gaps, not Phase 4 regressions — that first run had already proven, before failing, that all Phase 4 production Kotlin compiles cleanly via `assembleRelease --quiet` with zero errors): (1) `GuardDogSDKEngineTest.dnsMatchAloneNeverProducesThreatBlocked_onlyReportBlockedPacketCan` had a pre-existing Phase-3 bad assertion (`emitted.size == 0`, not accounting for the legitimate `RULE_BUNDLE_ACCEPTED` audit event) — narrowed to check only for absence of `THREAT_BLOCKED`/`THREAT_DETECTED`, matching 3 sibling tests already in the same file. (2) `backend/.env.example`'s M2 signing allow-list existed in the sandbox from Phase 1 but had never been committed (git status showed it modified across many prior sessions) — committed explicitly, fixing `executable-suites`' `test_website_gate_ruleset.py` 403s.
- Phase 5 (bridge/app wiring + local override store, complete — local checks green; native-gates CI run pending): `WebsiteGateOverrideStore` (`guarddog-vpn`: `ALLOW`/`NONE` only, no `BLOCK` value exists in the type) wired as an additive param into `SinkholeBindingStore.arm()` — an ALLOW override short-circuits before the rule-authority chain, so it can never arm a binding (4 new `SinkholeBindingStoreTest` cases + 6 new `WebsiteGateOverrideStoreTest` cases). `GuardDogExpoModule` gets 6 additive sync functions (`configureWebsiteGate`, `acceptWebsiteGateRuleBundle`, `getWebsiteGateStatus`, `setWebsiteGateAllowOverride`, `getWebsiteGateOverrides`, `clearWebsiteGateOverrides`); `GuardDogExpoModuleDefinitionTest` asserts the exact registered function set. New shared-contracts module `websiteGateOverrides.ts` (`guarddog-contracts/src`, synced to `frontend/src/contracts/shared`): durable override record `{ host, type: "allow", source: "user", decidedAt }` — `type` is a 1-value literal union, structurally excluding any "block" variant; bounded to `MAX_WEBSITE_GATE_OVERRIDES = 500` (oldest-evicted); 11 new `node --test` cases incl. an explicit test that a record tampered to `type: "block"` is rejected by the validator. New `frontend/src/sdk/websiteGateOverrides.ts` (thin AsyncStorage glue, the durable source of truth across restarts — the native store is session-only, matching `GuardDogVpnRuntime.config` re-push semantics). `GuardDogSecuritySDK.ts` gains the matching public wrapper surface + `getPlatformCapabilityProfile()` (returns `ANDROID_M2_CAPABILITIES` only when `getWebsiteGateStatus().dnsGatewayActive` is live-true, else M1 profile, `null` off-Android) — zero M1 methods touched. Verified locally: `guarddog-contracts` 30/30 `node --test` (was 19/19), ESLint clean, Expo web bundle rebuilds clean (953 modules), M1 harness screen boots with no console errors. **No `gh` CLI / git remote configured in this sandbox — the agent cannot trigger the `native-gates` GitHub Actions run itself; the user must push/trigger it (as with every prior phase) before Phase 5 can be called native-verified.** Consumer UI ("Apollo's Patrol") wiring stays out of scope per design doc §7 — no harness screen (`frontend/app/index.tsx`) was changed.
- Phase 5.1 (Android capability-truth correction, complete, `guarddog-contracts` only — NOT a shared-schema redesign): `ANDROID_M2_CAPABILITIES.dnsVisibility`/`domainVisibility` values were NOT flipped to `false` (would erase the milestone's real capability gain) and no field was added to `PlatformCapabilityProfile` (would be exactly the shared-schema change this correction must avoid). Instead: generalized the interface's doc comments to state every boolean is scoped to what the platform's own mechanism actually observes, expanded `ANDROID_M2_CAPABILITIES`'s comment to name the two real bypasses (Android Private DNS/DoT; app-embedded DoH — both invisible to Apollo's plaintext-UDP/53 sinkhole), and added a new exported, testable string constant `ANDROID_M2_DNS_VISIBILITY_SCOPE` stating the limitation for any consumer to quote verbatim. 1 new test (31/31 total, was 30/30) locks in the values stayed true/true and asserts the new constant names both bypasses + explicitly disclaims system-wide/universal coverage. No Kotlin/Swift file touched (this capability is synthesized JS-side from a live native flag). **Git note**: sandbox connected to `github.com/zelnix/Apollo`; verified via `git ls-remote` that this sandbox's prior HEAD (`908a8d0`) directly descends from the real `m2-native-acceptance` branch tip (`1071e733a0b0f0443c4fb834a1871d7d995d1ad3`, confirmed byte-identical) with 2 unpushed commits — no divergence; local branch renamed `main` → `m2-native-acceptance` to match, `origin` remote added (fetch/push not yet performed).
- Phase 5.2 (architectural duplication flagged, NOT reconciled): fetched `origin/main` and discovered it ships its OWN independent native Android VPN/DNS-filter implementation — `com.hucentai.apollosecurity` (`frontend/modules/apollo-security/android`, its own `PlatformCapabilityProfile`/`SecurityPlatformAdapter` TS contract using a tri-state `CapabilityLevel` + `ProtectionStatus.coverageScope: string[]`) — entirely separate from Gate Guard's `com.guarddog.*` stack in this repo. This was not previously known (main had only been reviewed for shared-contract changes, not identified as also having a second native VPN engine). Per explicit product decision: flagged as a duplication/coordination issue for the product owner to resolve in a future **Android Native Consolidation** milestone (current lean: Gate Guard becomes canonical, `com.hucentai.apollosecurity` retired/thinned — not decided or acted on). Main's `PlatformCapabilityProfile.ts`/`SecurityPlatformAdapter.ts` were deliberately NOT copied/wired into this branch. Gate Guard's own boolean-only capability contract was kept as-is (no redesign); instead `ANDROID_M2_DNS_VISIBILITY_SCOPE` now names the concrete scope tag `"dns:udp-53"` inline, and a new standalone constant `ANDROID_M2_DNS_COVERAGE_TAG = "dns:udp-53"` was added (same tag vocabulary as main's `coverageScope`, but not that type, not imported, not wired into anything shared). 2 new tests (32/32 total). Reminder recorded for Phase 6: the physical-device acceptance backend must run main's newly-landed Biting/Truth-of-State backend invariant, and Expo Go/web preview cannot validate the native DNS/VPN path — a generated native Android dev/release build is required.
- Phase 6 (native tests + physical-device proof): in-app harness screen COMPLETE + frontend-verified (screenshot + `testing_agent`, web preview only); actual physical-device run itself NOT started (device testing pending). **Phase 5 + 5.1 + 5.2 declared native-verified**: `native-gates` CI run [34295478834](https://github.com/zelnix/Apollo/actions/runs/34295478834) — Status: Success, commit `4966da4d45f02c293fd319afca5e425cce08c5d0` on `m2-native-acceptance`, all 6 jobs green (android, ios, executable-suites, android-dev-build, Gate Guard, android-startup-smoke), 20m23s, 5 artifacts (`android-dev-build` 42.5MB, `gate-guard` 66.1MB, `android-native-gate`, `android-startup-smoke`, `ios-native-gate`). This sandbox's local HEAD is exactly this commit (verified, clean tree). Fillable Phase 6 acceptance report template prepared at `docs/M2_PHASE6_ACCEPTANCE_TEMPLATE.md` — frozen invariant at top ("THREAT_BLOCKED is evidence-backed only"), full provenance/evidence-chain/PASS-FAIL-matrix (4 groups: positive enforcement, negative false-Biting, recovery/stop/revoke, Private DNS/DoT/DoH) + 3-way overall verdict (PASS / FAIL / PASS WITH DOCUMENTED CAPABILITY GAP).
  - **In-app runner built**: `frontend/app/phase6-acceptance.tsx` — mirrors the template field-for-field: auto-capture provenance (commit SHA/CI run id/APK SHA-256/version/build number via `readBuildProvenance()`, device-confirmed active native stack + manufacturer/model/OS/security patch via new Kotlin `getPhase6DeviceProvenance()`, ruleset/bundle info from the live backend bundle, network type via `expo-network`), manual-entry fields for what Android does not expose to third-party apps (Private DNS setting, branch/job/artifact names, backend provenance), "Activate Website Gate" (configure + accept bundle), guided Group-1 positive-enforcement runner (`runPositiveEnforcementTest` — real `fetch()`, observes the SDK's own event stream, never fabricates a THREAT_BLOCKED) and Group-2 negative-false-Biting runners (5 kinds: rule-match-alone, manual-override, local-analysis-only, gate-start-alone, failed-dns-forward) in `phase6WebsiteGateHarness.ts`, manual-step snapshot capture for Groups 3/4 (stop/revoke/restart/network-transition — Android doesn't let a third-party app trigger these itself, so the screen instructs the tester and then snapshots `getProtectionState()`/`getWebsiteGateStatus()` after each manual step), a fillable PASS/FAIL/CAPTURED/BYPASSED/UNOBSERVABLE matrix (4 groups, chip pickers, evidence-ref + notes per row), known-capability-gaps section (Private DNS/DoT + app-embedded DoH, pre-filled with the disclosed scope tags), and a 3-way verdict picker (PASS / FAIL / PASS WITH DOCUMENTED CAPABILITY GAP) — **the screen itself never computes or infers a verdict; every result/verdict field is exactly what the tester taps/types**. Export evolved from the original "copy to clipboard" idea to **Generate PDF + native share sheet** (`phase6AcceptanceReport.ts`, `expo-print` + `expo-file-system`, mirrors the template as styled HTML) — a more robust, shareable evidence artifact than clipboard text, kept as the primary export path.
  - **Bug fixed + wired this session**: `readBuildProvenance()` was called without `await` in the auto-capture mutation (commit/CI/APK fields would silently never populate on a real device); fixed. The screen was also an orphan route (no in-app link) — added an "Open Phase 6 Acceptance Report Runner" entry point on the main harness screen (`index.tsx`) with working back-navigation. Both verified via screenshot + `testing_agent` frontend pass (web preview): navigation, auto-capture (native-only fields correctly show "unavailable" placeholders off-device, non-native fields like network type populate), matrix/verdict chip toggling, text inputs, no crashes/console errors.
  - Ready to fill in for real during the actual physical-device run using commit `4966da4` (or a fresh Publish build with zero diff from it) as the provenance baseline. Physical device test itself still requires the user to run it (native Android build, not testable in Expo Go/web, no physical device access from this sandbox).
  - **CI regression found + fixed (2026-06, after user pushed commit `36be3a1` to GitHub)**: `native-gates` run [34332031276](https://github.com/zelnix/Apollo/actions/runs/34332031276) — `android` AND `ios` jobs BOTH failed at the identical first step (`yarn install --frozen-lockfile && ...`), before `expo prebuild` or any native-gate script ever ran; `executable-suites` ran (45s, no failure annotation); `android-dev-build`/`Gate Guard`/`android-startup-smoke` never ran (0s, workflow-dependency skip). **Root cause**: `frontend/package.json` requires `expo-network@~57.0.1` (added by the prior "ci: audit fixes — ... Expo SDK 57 patch alignment" session), but the committed `frontend/yarn.lock` was never updated with the matching resolved entry — `yarn install --frozen-lockfile` correctly refuses to silently mutate the lockfile in CI, so it hard-fails with `error Your lockfile needs to be updated`. **Not caused by this session's Phase 6 changes** (those two `.tsx` edits contain no dependency changes) — reproduced locally by stashing this session's already-present-but-uncommitted `yarn.lock` fix and re-running `yarn install --frozen-lockfile` against the exact committed HEAD (`36be3a1`), which failed identically; restoring the fix and re-running passes (`success Already up-to-date`). The `android-native-gate.txt`/`core-deps.txt`/`vpn-deps.txt` artifacts the user attached from that run are **stale evidence from the last PASSING run**, re-uploaded by an `if: always()` artifact step reading a committed `docs/evidence/` file that this run's script never actually regenerated (that script never ran — the job died one step earlier). **Fixed**: `frontend/yarn.lock` now carries the `expo-network@57.0.1` resolved entry; verified locally that `yarn install --frozen-lockfile` passes clean. User needs to push/trigger `native-gates` again to get a real (non-stale) android/ios gate result.

