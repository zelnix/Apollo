export type CaptureScope = "apollo_app" | "selected_app" | "full_display";
export type SessionState = "created" | "invited" | "helper_accepted" | "awaiting_capture_consent" | "connecting" | "active" | "paused" | "ending" | "ended" | "expired" | "revoked" | "failed";
export interface Capabilities { enabled: boolean; protocolVersion: number; unavailableReason: "configuration_missing" | "policy_rejected" | null; detail: string | null; supportedScopes: CaptureScope[]; microphone: "not_supported"; systemAudio: "not_supported"; remoteControl: "not_supported"; recording: "not_supported" }
export interface AssistSession { sessionId: string; relationshipId: string; captureScope: CaptureScope; microphoneRequested: false; state: SessionState; revision: number; generation: string; createdAt: string; invitationExpiresAt: string; activeExpiresAt?: string | null; activatedAt?: string | null; pausedAt?: string | null; endedAt?: string | null; endReason?: string | null; helperDisplayName: string; ownerDisplayName: string; supportedActions: string[] }
export interface SignalingTicket { ticket: string; expiresAt: string; sessionId: string; generation: string; role: "sharer" | "helper"; protocolVersion: number }