{
  "name": "apollo-desktop",
  "private": true,
  "version": "1.1.0",
  "description": "Apollo Windows/macOS host: Tauri v2 shell around the shared Expo web bundle with typed, allowlisted native commands.",
  "scripts": {
    "web:export": "cd ../frontend && EXPO_PUBLIC_APP_ENV=production npx expo export --platform web --output-dir ../desktop/dist",
    "dev": "tauri dev",
    "build": "yarn web:export && tauri build",
    "prepare:windows-native": "powershell -ExecutionPolicy Bypass -File scripts/build-windows-source.ps1",
    "prepare:macos-native": "sh scripts/prepare-macos-source.sh",
    "build:windows": "powershell -ExecutionPolicy Bypass -File scripts/build-windows-source.ps1",
    "build:macos": "sh scripts/build-macos-source.sh"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.5.0"
  }
}
